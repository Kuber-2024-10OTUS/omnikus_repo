apiVersion: v1
kind: Service
metadata:
  name: {{ include "homeworkapp.fullname" . }}
  labels:
    {{- include "homeworkapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "homeworkapp.selectorLabels" . | nindent 4 }}
