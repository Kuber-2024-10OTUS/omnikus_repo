apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "homeworkapp.fullname" . }}-test-connection"
  labels:
    {{- include "homeworkapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "homeworkapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
